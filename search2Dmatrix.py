# Define the 2D matrix
p = [[1, 2, 3], [4, 5, 6], [7, 8, 9]]

# Define the target element
target = 5

# Iterate through each row and column to find the target element
for row in range(len(p)):
    for col in range(len(p[row])):
        if p[row][col] == target


